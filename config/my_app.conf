{
    # Secret passphrase
    secrets => ['my_very_secure_secret_phrase_change_me'],
    
    # Logging
    log_level => 'debug',

    log_file => 'out',
    
    # Database configuration
    database => {
        driver   => 'mysql',
        host     => 'localhost',
        port     => 3306,
        database => 'message_log_app',
        username => 'root',
        password => '9638527410',
        options  => {
            mysql_enable_utf8 => 1,
            RaiseError => 1,
            PrintError => 0,
            AutoCommit => 1,
            mysql_auto_reconnect => 1,
        }
    },
    
    # Application settings
    app_name => 'GPB Application',
    
    # Default options
    default_options => {
        theme => 'light',
        language => 'ru',
        notifications => 1,
        items_per_page => 20
    }
}